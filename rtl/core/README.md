# Core integration work

The CPU top level will be written here after the frontend, rename, scheduling,
memory-ordering, and retirement interfaces are independently defined and tested.
The mixed-authorship course top level was intentionally not copied.
